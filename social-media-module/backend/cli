#!/usr/bin/env python3
"""
CLI Monitoring Tool Wrapper
Quick access to advanced monitoring commands
"""

import asyncio
import os
import sys

# Add backend directory to path
backend_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.append(backend_dir)

from monitoring.cli_monitor import main

if __name__ == "__main__":
    asyncio.run(main())
